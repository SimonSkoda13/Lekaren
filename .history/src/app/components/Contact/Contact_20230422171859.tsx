import React from "react";

export interface ContactProps {
    items: {
        title: string;
        description: string;
        icon: string;
    }[];
}

export const Contact = (props: ContactProps) => {
  const {} = props;
  return (
    <div className="
  );
};
